import 'package:flutter/material.dart';

class TicketHistoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ticket History Page'),
      ),
      body: Center(
        child: Text('Ticket History Page Content'),
      ),
    );
  }
}
